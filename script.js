const revealEls=document.querySelectorAll('.reveal');const io=new IntersectionObserver((entries)=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});revealEls.forEach(e=>io.observe(e));
const counters=document.querySelectorAll('[data-count]');let counted=false;const countIO=new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting)&&!counted){counted=true;counters.forEach(el=>{const target=+el.dataset.count;let start=0;const step=Math.max(1,Math.ceil(target/55));const timer=setInterval(()=>{start=Math.min(target,start+step);el.textContent=target===5000?(start/1000).toFixed(start<5000?1:0)+'K':start;if(start>=target)clearInterval(timer)},22)})}}, {threshold:.4});if(counters.length)countIO.observe(counters[0]);
const toast=document.getElementById('toast');document.querySelectorAll('.class-card button').forEach(btn=>btn.addEventListener('click',()=>{toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}));
const glow=document.querySelector('.cursor-glow');window.addEventListener('mousemove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const menu=document.querySelector('.menu');menu?.addEventListener('click',()=>{const nav=document.querySelector('.nav-links');nav.style.display=nav.style.display==='flex'?'none':'flex';if(nav.style.display==='flex'){nav.style.position='absolute';nav.style.top='65px';nav.style.left='15px';nav.style.right='15px';nav.style.flexDirection='column';nav.style.padding='20px';nav.style.background='#080d0e';nav.style.border='1px solid #293334';nav.style.gap='18px'}});
